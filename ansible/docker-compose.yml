services:
  app:
    build: .
  proxy:
    image: 'nginx:latest'
    volumes:
      - '/srv/nginx/ssl:/etc/ssl'
      - '/srv/nginx/nginx.conf:/etc/nginx/nginx.conf'
    ports:
      - '0.0.0.0:80:80'
      - '0.0.0.0:443:443'
